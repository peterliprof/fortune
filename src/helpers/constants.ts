export const dimension = 300;
export const edgeMargin = 2;

export const sizeFactor = 0.05;


export const actorDimension = dimension * sizeFactor;

export const targetDimension = dimension * sizeFactor;

export const leftInitActor = (dimension / 2) - (actorDimension / 2);
export const topInitActor = (dimension / 2) - (actorDimension / 2);
export const actorStepFactor = 2;

export const actorInterval = 10;
export const targetInterval = 30;
export const createTargetInterval = 500;

export const coinTotalNum = 100;
export const numberOfTargets = 8;
export const coinVal = 10;
export const coinBrokenVal = 2;

